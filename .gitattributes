# Normalise line endings so the same files check out cleanly on Windows and
# POSIX. The executable must keep LF or the shebang breaks on Linux.
* text=auto eol=lf

bin/agent-observer.js text eol=lf
*.sh text eol=lf
*.ps1 text eol=crlf

*.png binary
*.jpg binary
*.gif binary
